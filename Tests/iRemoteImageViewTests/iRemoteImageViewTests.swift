import XCTest
@testable import iRemoteImageView

final class iRemoteImageViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(iRemoteImageView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
